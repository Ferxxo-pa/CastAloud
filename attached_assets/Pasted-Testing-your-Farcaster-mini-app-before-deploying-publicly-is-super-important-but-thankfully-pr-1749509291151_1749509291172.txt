Testing your Farcaster mini app **before deploying publicly** is super important — but thankfully pretty easy. Here's a step-by-step guide:

---

### ✅ **1. Run Locally First**

Make sure your app works **on your local machine** like a regular web app:

```bash
npm run dev
# or
next dev
```

Visit:

```
http://localhost:3000
```

---

### ✅ **2. Use a Public Tunnel (for Mini App Testing)**

Warpcast and Farcaster mini apps need a **publicly accessible URL** to test inside the app.

Use **[ngrok](https://ngrok.com/)** or **[localtunnel](https://theboroer.github.io/localtunnel-www/)**:

```bash
npx localtunnel --port 3000
# or
ngrok http 3000
```

It’ll give you a URL like:

```
https://lively-badger.loca.lt
```

Use this as your temporary **mini app URL**.

---

### ✅ **3. Add `farcaster.json` to Your App**

You must host this file at:

```
https://yourdomain.com/.well-known/farcaster.json
```

Example `farcaster.json`:

```json
{
  "frame": {
    "version": "1.0.0",
    "name": "Cast Aloud",
    "homeUrl": "https://your-ngrok-url",
    "iconUrl": "https://your-ngrok-url/icon.png",
    "splashImageUrl": "https://your-ngrok-url/splash.png",
    "requiredChains": [],
    "requiredCapabilities": []
  }
}
```

---

### ✅ **4. Load it in Warpcast**

* Open [warpcast.com](https://warpcast.com) on desktop or mobile
* Create a **cast** with a button like:

  ```
  Check this out: https://lively-badger.loca.lt
  ```
* Tap the link → it should open your mini app *in-frame* as a modal

If the app doesn’t show up, double-check:

* The `.well-known/farcaster.json` is valid + reachable
* You’re using HTTPS
* There’s no CORS or crash in the console

---

### ✅ **5. Use Developer Mode on Warpcast (Optional)**

If you're on desktop:

* Right-click inside the mini app frame → Inspect
* You can use DevTools to test behavior live

---

### 🧪 Want a Full Deploy Preview?

When ready, deploy to:

* [Vercel](https://vercel.com) (best for Next.js/Farcaster devs)
* [Cloudflare Pages](https://pages.cloudflare.com/) (fast + free)
* [Netlify](https://netlify.com)

Then test the public version just like above.

---

Let me know if you want help validating your `farcaster.json` or need a staging checklist before going public.
